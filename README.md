# VibeCoded_HealthTracker1
This is a simple HealthTracker i built in a couple of hours using Vibe Coding; This is a streamlit prototype, so run "app.py" 
Convert to React and harden DB for holding actual patient data
